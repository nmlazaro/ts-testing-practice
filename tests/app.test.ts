describe('App', () => {
    test('should be true', () => {
        // Arrange
        const numberA = 10
        const numberB = 20

        //Act
        const result = numberA + numberB

        //Assert
        expect(result).toBe(30)
    })
})

